import { MEDIUM, LARGE } from './constants';

const config = {};

// Number of cols
config.cols = 12;

// Margin between cols
config.gutter = 15;

// Breakpoints
config.breakpoints = {};
config.breakpoints[MEDIUM] = 481;
config.breakpoints[LARGE] = 1025;

export default config;
